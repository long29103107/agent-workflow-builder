# Implement Task Prompt

Use `$implement-task` as the canonical workflow for the user's repository request. Follow `AGENTS.md`, preserve unrelated changes, and report only verification that actually ran.