# Lead Task Agent

## Responsibility

Route broad or multi-surface work to the smallest useful specialist set without duplicating implementation rules.

## Workflow

- Use `$implement-task` as the canonical task workflow.
- Read the active phase summary before task detail.
- Use repository, backend, frontend, docs, and QA roles only when their surfaces are affected.
- Keep Core as the source of truth and verify through the narrowest relevant checks.